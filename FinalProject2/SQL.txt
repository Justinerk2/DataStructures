CREATE TABLE tempData (
id int,
dateTime timestamp DEFAULT current_timestamp,
temp varchar(30));
INSERT INTO tempData
FROM VALUES (1,DEFAULT,75 degreesF);

creating table with 3 columns (ID number, TimeStamp, Temperature)